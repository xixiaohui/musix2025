<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Dark variant of the edge-to-edge theme. Uses DayNight parent for
         automatic dark mode support on API 29+. -->
    <style name="ThemeOverlay.CustomExoPlayer" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:enforceStatusBarContrast">false</item>
        <item name="android:enforceNavigationBarContrast">false</item>
        <item name="pause_icon" tools:ignore="PrivateResource">@drawable/exo_styled_controls_play</item>
    </style>
</resources>
